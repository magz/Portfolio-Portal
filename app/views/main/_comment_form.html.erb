
	
	<div class="add_comment_form">
	<%= form_for @new_comment, :html => {:id => "new_comment_form"}, :remote=> true, :url => { :action => "ajax_create_comment"} do |c| %>
			<textarea cols="30" id="comment_input" name="comment[body]" rows="5" resize="none"></textarea></br>
				<button type="submit" class="button" id="add_comment_button"></br>
				<button type="submit" class="button" id="add_comment_button"><span>Submit</span></button>
			<% end %>
			</div>
	
<script>
$(document).ready(function(){
	//alert("yoyoyoyo");
	$('.add_comment_form')
		.bind("ajax:complete", function(evt, data, status, xhr){
			var parsed_data = jQuery.parseJSON(data["responseText"]);
			//check this
			if(parsed_data["success"] == true) {
				$("#comments").html(parsed_data["comments_html"]);
				//$("#comment_input").val('')
			}
	    })

});

</script>